# 复制为 .env.local 并填入真实值。都不填也能跑(回退本地 JSON + 模板生成)。

# Supabase(在 supabase.com 建项目后,Project Settings → API 里拿)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Claude API(platform.claude.com → API Keys)。不填则简报用模板生成。
ANTHROPIC_API_KEY=
